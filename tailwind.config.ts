import { type Config } from 'tailwindcss';

export default {
  future: {
    hoverOnlyWhenSupported: true,
  },
  content: ['./src/**/*.tsx'],
  darkMode: ['class'],
  plugins: [],
} satisfies Config;
