import React from 'react';

function Sync() {
  return (
    <div>
      {/* 
      Here we will see the difference between local storage and account
      Have the option to sync the localstorage into the account (maybe other way around, but seems pointless) 
      Then we will also have the option to search for a lodestone character */}
    </div>
  );
}

export default Sync;
